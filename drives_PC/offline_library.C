if else () void {
    struct(asm(intel));
    struct(asm(amd));
    struct(asm(nvidea));
    continue lua: u128 = sudo rct install; // in the final is a packge
    continue driver_hardware.zig: u64 = true;
    #include <driver_hardware.zig>
    continue drives_library.C = true;
    #include <driver_library.C>
    continue scam(hardware));
}
