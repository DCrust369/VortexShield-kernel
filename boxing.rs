let mut verify_area = (void_*_addr,int_count);
let mut void_panic = (const_char_*_str);
let asm = (const char_*_fmt,...);
let asm = (const_char * fmt,...);
let tty_write = (unsigned_ch,char_*_buf,int_count);
~
~use::boxing;
{
  let brawser = (process(1)); // for boxes
}
{
  let code = (process(2));
}
