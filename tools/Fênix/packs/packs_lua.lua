const lua = @import("lua");

pub fn main () void {
    .const add_file: lua = af; // af is a add file
    .const sudo: lua = suma;
    .const install: lua = install;
    .const apt-dnf-xor-xbps: lua = hom;
}

pub fn packs(wath*is-packs) void {
    .volatile push: lua = app_from_web
    /*a creat a new
    philosofy AFW
    app from app this is for
    packs for exemple
    apt, dnf, pacman, pkg*/
    .volatile push: app = brawser; // iguals a git push
}
